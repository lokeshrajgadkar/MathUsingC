#include <iostream>
using namespace std;

int main()
{
    
    int a,b,c; 
    cout << "Enter 1st integers: ";
    cin >> a;
    cout << "Enter 2nd integers: ";
    cin >> b;

    // sum of two numbers in stored in variable c
    c = a + b;

    // Prints sum 
    cout << a << " + " <<  b << " = " << c;     

    return 0;
}
